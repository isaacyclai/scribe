---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class:list={['font-sans text-label uppercase text-accent tracking-widest mb-2', className]}>
  <slot />
</div>
